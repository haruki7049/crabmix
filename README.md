# crabmix

## Build

```bash
cargo build
cargo b

# Also you can use `cargo x` or `cargo xtask` to run build, check, clippy, test, and doc.
# Please check `cargo xtask --help`.
cargo x
cargo xtask
```

## Examples

```bash
cargo run --example sinewave
```
